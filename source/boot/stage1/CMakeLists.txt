find_program(OS_STAGE1_NASM_EXECUTABLE nasm REQUIRED)

set(
    OS_STAGE1_SOURCE
    "${CMAKE_CURRENT_SOURCE_DIR}/src/entry.asm"
)
set(
    OS_STAGE1_KERNEL_LOADER_SOURCE
    "${CMAKE_CURRENT_SOURCE_DIR}/src/kernel_loader.asm"
)
set(
    OS_STAGE1_MEMORY_MAP_SOURCE
    "${CMAKE_CURRENT_SOURCE_DIR}/src/memory_map.asm"
)
set(
    OS_STAGE1_KERNEL_LOADER_CONSTANTS
    "${CMAKE_CURRENT_SOURCE_DIR}/include/kernel_loader.inc"
)
set(
    OS_STAGE1_BINARY
    "${CMAKE_CURRENT_BINARY_DIR}/generated/stage1.bin"
)

add_custom_command(
    OUTPUT "${OS_STAGE1_BINARY}"
    COMMAND
        "${CMAKE_COMMAND}"
        -E
        make_directory
        "${CMAKE_CURRENT_BINARY_DIR}/generated"
    COMMAND
        "${OS_STAGE1_NASM_EXECUTABLE}"
        -I
        "${CMAKE_CURRENT_SOURCE_DIR}/include/"
        -I
        "${CMAKE_CURRENT_SOURCE_DIR}/src/"
        -f
        bin
        -o
        "${OS_STAGE1_BINARY}"
        "${OS_STAGE1_SOURCE}"
    DEPENDS
        "${OS_STAGE1_SOURCE}"
        "${OS_STAGE1_KERNEL_LOADER_SOURCE}"
        "${OS_STAGE1_MEMORY_MAP_SOURCE}"
        "${OS_STAGE1_KERNEL_LOADER_CONSTANTS}"
    COMMENT "汇编自研 Stage 1 模式切换与 ELF64 内核加载器"
    VERBATIM
)

add_custom_target(os_stage1_binary DEPENDS "${OS_STAGE1_BINARY}")

function(
    os_add_stage1_failure_binary
    OS_STAGE1_FAILURE_BINARY
    OS_STAGE1_FAILURE_DEFINITION
    OS_STAGE1_FAILURE_COMMENT
)
    add_custom_command(
        OUTPUT "${OS_STAGE1_FAILURE_BINARY}"
        COMMAND
            "${CMAKE_COMMAND}"
            -E
            make_directory
            "${CMAKE_CURRENT_BINARY_DIR}/generated"
        COMMAND
            "${OS_STAGE1_NASM_EXECUTABLE}"
            -I
            "${CMAKE_CURRENT_SOURCE_DIR}/include/"
            -I
            "${CMAKE_CURRENT_SOURCE_DIR}/src/"
            -D
            "${OS_STAGE1_FAILURE_DEFINITION}=1"
            -f
            bin
            -o
            "${OS_STAGE1_FAILURE_BINARY}"
            "${OS_STAGE1_SOURCE}"
        DEPENDS
            "${OS_STAGE1_SOURCE}"
            "${OS_STAGE1_KERNEL_LOADER_SOURCE}"
            "${OS_STAGE1_MEMORY_MAP_SOURCE}"
            "${OS_STAGE1_KERNEL_LOADER_CONSTANTS}"
        COMMENT "${OS_STAGE1_FAILURE_COMMENT}"
        VERBATIM
    )
endfunction()

set(
    OS_STAGE1_KERNEL_ATA_TIMEOUT_BINARY
    "${CMAKE_CURRENT_BINARY_DIR}/generated/stage1_kernel_ata_timeout.bin"
)
set(
    OS_STAGE1_KERNEL_ATA_ERROR_BINARY
    "${CMAKE_CURRENT_BINARY_DIR}/generated/stage1_kernel_ata_error.bin"
)
set(
    OS_STAGE1_MEMORY_MAP_INVALID_BINARY
    "${CMAKE_CURRENT_BINARY_DIR}/generated/stage1_memory_map_invalid.bin"
)
os_add_stage1_failure_binary(
    "${OS_STAGE1_KERNEL_ATA_TIMEOUT_BINARY}"
    OS_STAGE1_TEST_FORCE_KERNEL_ATA_TIMEOUT
    "汇编 Stage 1 Kernel ATA 超时测试镜像"
)
os_add_stage1_failure_binary(
    "${OS_STAGE1_KERNEL_ATA_ERROR_BINARY}"
    OS_STAGE1_TEST_FORCE_KERNEL_ATA_ERROR
    "汇编 Stage 1 Kernel ATA 设备错误测试镜像"
)
os_add_stage1_failure_binary(
    "${OS_STAGE1_MEMORY_MAP_INVALID_BINARY}"
    OS_STAGE1_TEST_FORCE_MEMORY_MAP_INVALID
    "汇编 Stage 1 物理内存图发现失败测试镜像"
)

function(
    os_add_stage1_failure_disk
    OS_STAGE1_FAILURE_DISK_IMAGE
    OS_STAGE1_FAILURE_BINARY
)
    get_filename_component(
        OS_STAGE1_FAILURE_OUTPUT_DIRECTORY
        "${OS_STAGE1_FAILURE_DISK_IMAGE}"
        DIRECTORY
    )
    add_custom_command(
        OUTPUT "${OS_STAGE1_FAILURE_DISK_IMAGE}"
        COMMAND
            "${Python3_EXECUTABLE}"
            "${PROJECT_SOURCE_DIR}/tools/os.py"
            create-boot-images
            "${OS_STAGE1_FAILURE_BINARY}"
            "${OS_KERNEL_PAYLOAD_ELF_PATH}"
            "${OS_STAGE1_FAILURE_OUTPUT_DIRECTORY}"
            "${OS_DISK_IMAGE_SIZE_BYTES}"
        DEPENDS
            "${OS_STAGE1_FAILURE_BINARY}"
            "${OS_KERNEL_PAYLOAD_ELF_PATH}"
            "${PROJECT_SOURCE_DIR}/tools/os.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_layout.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_elf.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/rootfs_v2.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/sparse_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/stage1_image.py"
        COMMENT "生成 Stage 1 Kernel ATA 失败路径磁盘镜像"
        VERBATIM
    )
endfunction()

os_add_stage1_failure_disk(
    "${OS_STAGE1_KERNEL_ATA_TIMEOUT_DISK_IMAGE}"
    "${OS_STAGE1_KERNEL_ATA_TIMEOUT_BINARY}"
)
os_add_stage1_failure_disk(
    "${OS_STAGE1_KERNEL_ATA_ERROR_DISK_IMAGE}"
    "${OS_STAGE1_KERNEL_ATA_ERROR_BINARY}"
)
os_add_stage1_failure_disk(
    "${OS_STAGE1_MEMORY_MAP_INVALID_DISK_IMAGE}"
    "${OS_STAGE1_MEMORY_MAP_INVALID_BINARY}"
)

function(
    os_add_kernel_fault_disk
    OS_KERNEL_FAULT_DISK_IMAGE
    OS_KERNEL_FAULT_ELF
    OS_KERNEL_FAULT_COMMENT
)
    get_filename_component(
        OS_KERNEL_FAULT_OUTPUT_DIRECTORY
        "${OS_KERNEL_FAULT_DISK_IMAGE}"
        DIRECTORY
    )
    add_custom_command(
        OUTPUT "${OS_KERNEL_FAULT_DISK_IMAGE}"
        COMMAND
            "${Python3_EXECUTABLE}"
            "${PROJECT_SOURCE_DIR}/tools/os.py"
            create-boot-images
            "${OS_STAGE1_BINARY}"
            "${OS_KERNEL_FAULT_ELF}"
            "${OS_KERNEL_FAULT_OUTPUT_DIRECTORY}"
            "${OS_DISK_IMAGE_SIZE_BYTES}"
        DEPENDS
            "${OS_STAGE1_BINARY}"
            "${OS_KERNEL_FAULT_ELF}"
            "${PROJECT_SOURCE_DIR}/tools/os.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_layout.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_elf.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/rootfs_v2.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/sparse_image.py"
            "${PROJECT_SOURCE_DIR}/tools/os_tools/stage1_image.py"
        COMMENT "${OS_KERNEL_FAULT_COMMENT}"
        VERBATIM
    )
endfunction()

os_add_kernel_fault_disk(
    "${OS_KERNEL_INVALID_OPCODE_DISK_IMAGE}"
    "${OS_KERNEL_INVALID_OPCODE_PAYLOAD_ELF_PATH}"
    "生成非法指令异常注入磁盘镜像"
)
os_add_kernel_fault_disk(
    "${OS_KERNEL_PAGE_FAULT_DISK_IMAGE}"
    "${OS_KERNEL_PAGE_FAULT_PAYLOAD_ELF_PATH}"
    "生成页故障异常注入磁盘镜像"
)
os_add_kernel_fault_disk(
    "${OS_KERNEL_WRITE_PROTECTION_DISK_IMAGE}"
    "${OS_KERNEL_WRITE_PROTECTION_PAYLOAD_ELF_PATH}"
    "生成内核写保护故障注入磁盘镜像"
)
os_add_kernel_fault_disk(
    "${OS_KERNEL_USER_INVALID_OPCODE_DISK_IMAGE}"
    "${OS_KERNEL_USER_INVALID_OPCODE_PAYLOAD_ELF_PATH}"
    "生成 Ring 3 非法指令隔离磁盘镜像"
)
os_add_kernel_fault_disk(
    "${OS_KERNEL_USER_PAGE_FAULT_DISK_IMAGE}"
    "${OS_KERNEL_USER_PAGE_FAULT_PAYLOAD_ELF_PATH}"
    "生成 Ring 3 页故障隔离磁盘镜像"
)
os_add_kernel_fault_disk(
    "${OS_KERNEL_USER_INVALID_ELF_DISK_IMAGE}"
    "${OS_KERNEL_USER_INVALID_ELF_PAYLOAD_ELF_PATH}"
    "生成 Ring 3 非法 ELF 拒绝磁盘镜像"
)

set(
    OS_STAGE1_CORE_TOOL_NAMES
    help
    echo
    cat
    wc
    head
    tee
    true
    false
    pwd
    ls
    stat
    mkdir
    write
    touch
    rm
    rmdir
    mv
    truncate
    sync
    basename
    dirname
    cp
    seq
    uptime
    ps
    free
    uname
    mounts
    resources
    sleep
    kill
    id
)
set(OS_STAGE1_REQUIRED_CORE_TOOL_COUNT 32)
list(LENGTH OS_STAGE1_CORE_TOOL_NAMES OS_STAGE1_CORE_TOOL_COUNT)
if(NOT OS_STAGE1_CORE_TOOL_COUNT EQUAL OS_STAGE1_REQUIRED_CORE_TOOL_COUNT)
    message(FATAL_ERROR "rootfs 核心工具清单必须精确包含 32 个独立路径")
endif()
set(OS_STAGE1_CORE_TOOL_INSTALL_ARGUMENTS)
foreach(OS_STAGE1_CORE_TOOL_NAME IN LISTS OS_STAGE1_CORE_TOOL_NAMES)
    list(
        APPEND
        OS_STAGE1_CORE_TOOL_INSTALL_ARGUMENTS
        --rootfs-file
        "/bin/${OS_STAGE1_CORE_TOOL_NAME}=${OS_USER_CORE_TOOL_ELF_PATH}"
    )
endforeach()

set(
    OS_STAGE1_ROOTFS_INSTALL_ARGUMENTS
    --rootfs-file
    "/sbin/init=${OS_USER_INIT_ELF_PATH}"
    --rootfs-file
    "/bin/sh=${OS_USER_SHELL_ELF_PATH}"
    ${OS_STAGE1_CORE_TOOL_INSTALL_ARGUMENTS}
    --rootfs-file
    "/bin/smoke=${OS_USER_SMOKE_ELF_PATH}"
    --rootfs-file
    "/bin/orphan_parent=${OS_USER_ORPHAN_PARENT_ELF_PATH}"
    --rootfs-file
    "/bin/orphan_child=${OS_USER_ORPHAN_CHILD_ELF_PATH}"
    --rootfs-file
    "/bin/argument_probe=${OS_USER_ARGUMENT_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/exec_probe=${OS_USER_EXEC_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/exec_target=${OS_USER_EXEC_TARGET_ELF_PATH}"
    --rootfs-file
    "/bin/fs_probe=${OS_USER_FILE_SYSTEM_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/memory_probe=${OS_USER_MEMORY_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/fork_probe=${OS_USER_FORK_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/thread_probe=${OS_USER_THREAD_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/time_probe=${OS_USER_TIME_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/signal_probe=${OS_USER_SIGNAL_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/memory_guard_probe=${OS_USER_MEMORY_GUARD_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/memory_protection_probe=${OS_USER_MEMORY_PROTECTION_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/tool_probe=${OS_USER_TOOL_PROBE_ELF_PATH}"
    --rootfs-file
    "/bin/truncated.elf=${OS_USER_TRUNCATED_ELF_PATH}"
)

add_custom_command(
    OUTPUT
        "${OS_BOOT_DISK_IMAGE}"
        "${OS_STAGE1_INVALID_HEADER_DISK_IMAGE}"
        "${OS_STAGE1_INVALID_CHECKSUM_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_HEADER_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_CHECKSUM_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_ELF_DISK_IMAGE}"
    COMMAND
        "${Python3_EXECUTABLE}"
        "${PROJECT_SOURCE_DIR}/tools/os.py"
        create-boot-images
        "${OS_STAGE1_BINARY}"
        "${OS_KERNEL_PAYLOAD_ELF_PATH}"
        "${OS_IMAGE_OUTPUT_DIRECTORY}"
        "${OS_DISK_IMAGE_SIZE_BYTES}"
        ${OS_STAGE1_ROOTFS_INSTALL_ARGUMENTS}
    DEPENDS
        "${OS_STAGE1_BINARY}"
        "${OS_KERNEL_PAYLOAD_ELF_PATH}"
        "${OS_USER_INIT_ELF_PATH}"
        "${OS_USER_SHELL_ELF_PATH}"
        "${OS_USER_CORE_TOOL_ELF_PATH}"
        "${OS_USER_SMOKE_ELF_PATH}"
        "${OS_USER_ORPHAN_PARENT_ELF_PATH}"
        "${OS_USER_ORPHAN_CHILD_ELF_PATH}"
        "${OS_USER_ARGUMENT_PROBE_ELF_PATH}"
        "${OS_USER_EXEC_PROBE_ELF_PATH}"
        "${OS_USER_EXEC_TARGET_ELF_PATH}"
        "${OS_USER_FILE_SYSTEM_PROBE_ELF_PATH}"
        "${OS_USER_MEMORY_PROBE_ELF_PATH}"
        "${OS_USER_FORK_PROBE_ELF_PATH}"
        "${OS_USER_THREAD_PROBE_ELF_PATH}"
        "${OS_USER_TIME_PROBE_ELF_PATH}"
        "${OS_USER_SIGNAL_PROBE_ELF_PATH}"
        "${OS_USER_MEMORY_GUARD_PROBE_ELF_PATH}"
        "${OS_USER_MEMORY_PROTECTION_PROBE_ELF_PATH}"
        "${OS_USER_TOOL_PROBE_ELF_PATH}"
        "${OS_USER_TRUNCATED_ELF_PATH}"
        "${PROJECT_SOURCE_DIR}/tools/os.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_layout.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/boot_image.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_elf.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/kernel_image.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/rootfs_v2.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/sparse_image.py"
        "${PROJECT_SOURCE_DIR}/tools/os_tools/stage1_image.py"
    COMMENT "生成包含 Stage 1 与 Kernel ELF 的正常和损坏磁盘镜像"
    VERBATIM
)

add_custom_target(
    os_stage1_images
    DEPENDS
        "${OS_BOOT_DISK_IMAGE}"
        "${OS_STAGE1_INVALID_HEADER_DISK_IMAGE}"
        "${OS_STAGE1_INVALID_CHECKSUM_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_HEADER_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_CHECKSUM_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_ELF_DISK_IMAGE}"
        "${OS_STAGE1_KERNEL_ATA_TIMEOUT_DISK_IMAGE}"
        "${OS_STAGE1_KERNEL_ATA_ERROR_DISK_IMAGE}"
        "${OS_STAGE1_MEMORY_MAP_INVALID_DISK_IMAGE}"
        "${OS_KERNEL_INVALID_OPCODE_DISK_IMAGE}"
        "${OS_KERNEL_PAGE_FAULT_DISK_IMAGE}"
        "${OS_KERNEL_WRITE_PROTECTION_DISK_IMAGE}"
        "${OS_KERNEL_USER_INVALID_OPCODE_DISK_IMAGE}"
        "${OS_KERNEL_USER_PAGE_FAULT_DISK_IMAGE}"
        "${OS_KERNEL_USER_INVALID_ELF_DISK_IMAGE}"
)

# Makefile 生成器不会把其他二进制目录中的文件级自定义命令自动提升为目标依赖。
# 显式声明 ELF 目标可保证全新构建或清理产物后，始终先链接所有 Kernel ELF，
# 再进入磁盘打包规则，避免出现 “No rule to make target kernel.elf”。
add_dependencies(
    os_stage1_images
    os_kernel_payload_elf
    os_kernel_invalid_opcode_payload_elf
    os_kernel_page_fault_payload_elf
    os_kernel_write_protection_payload_elf
    os_kernel_user_invalid_opcode_payload_elf
    os_kernel_user_page_fault_payload_elf
    os_kernel_user_invalid_elf_payload_elf
    os_user_init_elf
    os_user_shell_elf
    os_user_core_tool_elf
    os_user_smoke_elf
    os_user_orphan_parent_elf
    os_user_orphan_child_elf
    os_user_argument_probe_elf
    os_user_exec_probe_elf
    os_user_exec_target_elf
    os_user_file_system_probe_elf
    os_user_memory_probe_elf
    os_user_fork_probe_elf
    os_user_thread_probe_elf
    os_user_time_probe_elf
    os_user_signal_probe_elf
    os_user_memory_guard_probe_elf
    os_user_memory_protection_probe_elf
    os_user_tool_probe_elf
    os_user_truncated_elf
)
