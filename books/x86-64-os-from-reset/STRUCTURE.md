# 教材知识顺序与素材边界

本文件写给书稿维护者，用来记录章节依赖和素材归属。它不是正文模板。文字
编辑方法见 `EDITORIAL.md`。教材以 STUOS v2.0 的实现、构建产物和运行结果
为准；调整书稿结构不得改变 Kernel、ABI、rootfs 盘面或项目版本。

## 为什么采用 20 章主线和 4 章硬件附录

31 章版本把每一个可独立验收的机制都提升成章，解决了旧稿大章内部难以导航
的问题，却让纸面目录过于零碎。物理页和页表、调度和系统调用、fd 和 VFS
本来就是读者需要连续追踪的因果链，不必在链条中间反复翻到新章。

本版压缩为 20 个主线章。合并只改变章级边界，不删除正文：

- 电气与数字逻辑合成从电压到状态的一章；
- 软件职责与 x86 汇编合成进入真实启动代码前的一章；
- ROM 生成与 UART 首次输出合成一个可观察实验；
- 物理页与页表、KVA/栈与堆分别合并；
- 调度与系统调用、KernelObject 与 VFS、exec 与 VMA/fork 分别合并；
- 盘面与 rootfs、ATA/writeback 与 journal、等待与 signal/TTY 分别合并。

前三章构成从物理状态到第一条指令的完整地基。正文保留 bit、byte、补码、
端序、地址、KCL、RC、输入输出引脚、推挽/开漏/三态、组合逻辑、锁存器、
D 触发器、setup/hold、基础 CDC、寄存器、教学指令集、逐周期执行、总线
事务、存储器、地址译码与 ROM 取指。测量误差、器件选型、开关电源、静态
时序例外、复杂 CDC、PCB、DRAM 训练和实体整机资料进入四个硬件深化附录。
内容按“必须理解、深入理解、项目连接、动手实验、暂时跳过”标记阅读层次，
不再用几页摘要代替知识地基。

## 六部分各做什么

| 部分 | 章节 | 读者遇到的问题 | 主要机制 |
|---|---:|---|---|
| 物理世界到计算机 | 1–3 | 0 和 1 怎样成为可取指的机器 | bit、byte、电压、逻辑门、寄存器、CPU、RAM、ROM |
| 读懂 x86 并进入内核 | 4–8 | 源码怎样从复位现场运行到 64 位 C++ | 软件层次、Intel 汇编、复位、平台 I/O、ROM、UART、Loader |
| 内核接管机器 | 9–12 | 异常、地址和设备怎样进入内核 | GDT/TSS/IDT、buddy、分页、KVA、heap、IRQ、Ring 3 |
| 程序共享 CPU 和对象 | 13–15 | 线程、fd、路径和地址空间怎样协作 | 调度、SYSCALL、FileTable、VFS、PID 1、exec、VMA、fork |
| 数据持久化 | 16–17 | 一个字节怎样跨重启保留 | inode、rootfs、ATA IRQ、writeback、ordered journal |
| 等待、用户环境与继续开发 | 18–20 | 程序怎样等待、通信并接受终端控制 | pipe、futex、signal、TTY、Shell、ABI、源码导航与测试 |

## 20 章的学习动作

| 章 | 主题 | 读者要完成的动作 |
|---:|---|---|
| 1 | 先认识 bit、byte 和地址 | 换算位宽、十六进制和半开地址区间 |
| 2 | 电压怎样变成逻辑和状态 | 从闭合电路画到组合逻辑、触发器和寄存器 |
| 3 | CPU 怎样读到第一条指令 | 从 PC 和地址总线追到 ROM 返回的字节 |
| 4 | 从程序需要走到 x86 指令 | 区分软件层次并跟踪汇编状态变化 |
| 5 | x86 为什么从实模式醒来 | 手算分段地址、复位地址与 A20 回绕 |
| 6 | CPU 怎样找到内存和设备 | 区分内存事务、MMIO 与 Port I/O |
| 7 | 把源码变成能够输出的 ROM | 从符号追到 ROM 字节并输出 UART 日志 |
| 8 | 从磁盘进入 64 位内核 | 检查 ATA、ELF、A20、GDT、PAE 与模式切换 |
| 9 | 内核怎样接住第一次异常 | 解码 GDT、TSS、IDT 和 176 字节现场 |
| 10 | 物理页怎样变成受保护的虚拟地址 | 分配页帧并手算四级页表和权限 |
| 11 | 内核怎样分配虚拟空间和对象 | 建立动态栈、堆和类型缓存并检查回滚 |
| 12 | 中断怎样把设备和用户程序带进内核 | 从 IRQ 追到驱动、EOI 和 Ring 3 |
| 13 | 线程怎样轮流运行并进入系统调用 | 观察切换现场并沿 SYSCALL 追到 CpuLocal |
| 14 | 文件描述符和路径怎样找到同一个对象 | 从 fd 与路径追到 FileDescription 和 VFS |
| 15 | 进程怎样装入程序、缺页并完成 fork | 跟踪 exec、VMA、文件缺页和 COW |
| 16 | 扇区怎样组成 rootfs 的目录和文件 | 解码盘面、多级块索引、稀疏文件和 fsck |
| 17 | 数据怎样写入磁盘并在断电后恢复 | 观察 IRQ14、writeback、FLUSH 和 replay |
| 18 | 线程怎样等待、收信号并接受终端控制 | 运行 pipe、futex、signal、TTY 和作业控制 |
| 19 | 用户程序怎样使用系统服务 | 运行 Shell、devfs、procfs 与自动键盘会话 |
| 20 | 回看整个系统并准备下一次修改 | 定位接口、选择测试并设计一项增量 |

## 前三章的阅读层次

前三章直接输入 `source/latex/foundations/` 中的完整基础材料，沿五步推进：

1. bit、byte、十六进制、补码、端序、地址和 ROM 文件偏移；
2. 电压参考、闭合回路、引脚输出结构、电源、时钟和复位；
3. 逻辑门、组合逻辑、锁存器、D 触发器、寄存器和状态机；
4. 教学指令集、逐周期 CPU、RAM、ROM、互连和取指；
5. 把教学模型映射到 x86 复位地址与真实项目产物。

每章仍保持 4–6 个编号主节，细部用 topic 书签导航。正文保留理解下一层所需
的公式、波形、状态表、手算和推导；附录只接收不会阻断主线的仪器误差、
器件选型、静态时序例外、复杂 CDC、PCB、高速接口、DRAM 训练和实体整机
资料。`foundations/mainline/` 保留为简明素材来源，不再直接作为成书正文
入口。

## 系统机制怎样合并而不失去导航

章节边界按第一次可观察结果确定：

- 第 10 章从物理页分配连续走到页表权限，不在中途换章；
- 第 11 章从 KVA 和动态栈连续走到堆、类型缓存和回收；
- 第 13 章把调度现场与系统调用入口放在同一条 Thread 控制流中；
- 第 14 章从 fd 和路径两个方向走到同一 VFS 对象；
- 第 15 章从 exec 建立映像，继续观察缺页与 fork/COW；
- 第 16 章同时解释基础盘面和 rootfs v2 的大文件扩展；
- 第 17 章从 ATA 完成、writeback 连续走到 journal replay；
- 第 18 章先建立等待原语，再用 signal 和 TTY 组合它。

合并后的原有小节仍以 `\topic` 生成 PDF 书签并进入页眉。纸面目录只展示
4–6 个主节，手机书签仍可直接跳到 buddy、TLB、CpuLocal、COW 或 replay。

内部作者仍可检查历史、硬件事实、所有权、正常路径、失败路径、代码和测试，
但这些项目不能机械变成每章相同的七段正文。A20 沿地址回绕讲，UART 沿第一
个字符讲，fork 沿父子写入冲突讲，journal 沿断电位置讲。

## 源码走读和导航

- 一次正文代码清单只解释一个动作；通常控制在 5–40 行。
- ROM 完整源码仍保留仓库路径，但正文分别展示复位跳转、入口状态、UART
  初始化、字符串遍历和 THRE 超时。
- 每章开头用 `\chapterroute` 给出 4–6 个阅读节点。
- `\section` 进入纸面目录；`\topic` 不编号，但进入 PDF 书签并显示在奇数页
  页眉。手机阅读可以跳到 GDT、buddy、COW 或 replay。
- 生产源码行数属于维护统计，不进入教材正文。

## 字体和排版

- 正文、章、节、路线框、页眉和代码统一使用 Maple Mono NF CN。
- 标题靠字号、粗体、颜色与上下留白建立层级；代码靠等宽排版、底色和边框
  与正文区分。
- 每章 4–6 个编号主节；不得用大量编号二级标题切碎叙事。
- 所有语义表格使用实线网格环境；每行、每列和外框都有实线。
- 插图构建后必须渲染检查节点重叠、箭头穿字、标签压线和手机可读性。

## 全仓资料怎样进入书稿

- `docs/background_knowledge.md` 和 `docs/learning/background/` 提供历史与
  前置概念；
- `docs/learning/00-*.md` 与 `01`–`27` 分册提供真实演进与实验事实；
- `docs/architecture.md`、`docs/modules/` 与 ADR 提供当前职责和取舍；
- `docs/hardware/`、器件资料和矢量图提供寄存器、引脚与电气事实；
- `docs/testing.md`、`debugging.md`、`logging.md` 提供反例和故障定位；
- Intel SDM、ELF 规范与设备资料用于最后一轮技术审校。

正文按因果依赖重组，不按文件逐段复制。书末
`learning-integration-map.tex` 保留从原学习分册反查章节的入口。

## 构建门禁

1. 20 个主线章与 4 个硬件附录的所有 `\input` 必须可达；
2. 每章必须有 4–6 个编号主节；
3. 图像只接受 TikZ/CircuitikZ 或矢量 PDF；
4. 表格只接受三种 `osgrid*` 实线网格环境；
5. XeLaTeX 不得有未定义引用、正文越界或致命错误；
6. 标题、正文和代码均使用 Maple Mono，并以字号、字重、颜色和边框区分职责；
7. 核心代码统计只计生产 `.asm`、`.cpp`、`.hpp`，且不进入正文；
8. 本轮只修改教材；Kernel、ABI、盘面和网站不得变化；
9. 源 PDF 与手机导出件必须具有相同 SHA-256。
