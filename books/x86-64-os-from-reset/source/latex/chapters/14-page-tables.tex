\chapter{页表怎样翻译和保护地址}

给定一个虚拟地址，我们逐级取出 PML4、PDPT、PD 和 PT 索引，找到最终页帧，
再检查每一级的 Present、Writable、User 和 NX。随后故意写入只读页，确认
权限来自处理器的真实页表检查。

\chapterroute{direct map $\rightarrow$ 四级索引 $\rightarrow$ 层级权限
$\rightarrow$ TLB 刷新 $\rightarrow$ 写保护页故障。}

\input{deepening/ch08-page-tables}
