\chapter{扇区怎样组成目录和文件}

块设备只读写编号扇区。文件系统再用超级块、位图、inode、目录项和数据块建立
名字与内容。本章先完成一个尺寸较小、可以手工画完的格式，再用重挂载检查
写入是否真的离开内存。ATA 端口、IRQ14 和超时竞争留到第 26 章。

\chapterroute{分区中的扇区 $\rightarrow$ 超级块与 inode $\rightarrow$ bitmap
$\rightarrow$ 目录路径 $\rightarrow$ 写入、同步和重新挂载。}

\input{deepening/ch13-file-system}
