\chapter{扇区怎样组成目录和文件}

ATA 只读写编号扇区。文件系统再用超级块、位图、inode、目录项和数据块建立
名字与内容。本章先完成一个尺寸较小、可以手工画完的格式，再用重挂载检查
写入是否真的离开内存。

\chapterroute{ATA 扇区 $\rightarrow$ 超级块与 inode $\rightarrow$ bitmap
$\rightarrow$ 目录路径 $\rightarrow$ 写入、同步和重新挂载。}

\input{topics/ch13-persistence-introduction}
\input{deepening/ch13-file-system}
