\chapter{路径怎样穿过 VFS 和挂载点}

路径 \filepath{/tmp/session} 不是一个磁盘地址。解析器从根或当前目录逐个
处理组件，跨过挂载点，再把操作交给具体后端。本章用 memfs 先验证路径、
目录、挂载和文件增长，不混入磁盘断电问题。

\chapterroute{VFS 对象 $\rightarrow$ 路径组件 $\rightarrow$ 挂载进入与退出
$\rightarrow$ memfs 文件和目录 $\rightarrow$ 双后端测试。}

\input{deepening/ch11-vfs-and-memfs}
