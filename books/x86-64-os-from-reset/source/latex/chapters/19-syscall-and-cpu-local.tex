\chapter{系统调用怎样找到当前线程}

\code{syscall} 不会自动替内核选择栈，也不会替软件保存全部用户寄存器。
入口先通过 GS 基址找到 CpuLocal，再切到当前 Thread 的内核栈，构造统一的
UserContext。返回前还要验证 RIP、RSP、RFLAGS 和段选择子。

\chapterroute{处理器能力 $\rightarrow$ STAR/LSTAR/FMASK $\rightarrow$
CpuLocal 与 SWAPGS $\rightarrow$ UserContext $\rightarrow$ SYSRET/IRETQ
返回检查。}

\input{deepening/ch10-cpu-local-and-syscall}
