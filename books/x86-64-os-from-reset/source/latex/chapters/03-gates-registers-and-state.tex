\chapter{机器怎样记住一个状态}

两个开关接到 AND 门，只有同时打开时输出才为高电平；断开输入后，普通逻辑门
不会记得刚才发生了什么。触发器增加了“保存上一次状态”的能力，时钟则决定
何时更新。我们从这两个电路出发，逐步搭出寄存器、计数器、加法器和状态机。

\chapterroute{逻辑门与真值表 $\rightarrow$ 加法器、MUX 和译码器
$\rightarrow$ 反馈、锁存器与 D 触发器 $\rightarrow$ 时序、亚稳态和基础
CDC $\rightarrow$ 寄存器、程序计数器与状态机。}

\input{foundations/digital-logic-clock-and-state}
