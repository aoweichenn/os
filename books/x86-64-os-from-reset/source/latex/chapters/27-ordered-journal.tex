\chapter{断电后怎样重放文件系统日志}

创建文件会修改多块元数据，电源可能在任意两次写入之间消失。ordered journal
先记录 descriptor、payload 和 commit，再更新正式位置。重新启动时只接受
旧状态或已提交的新状态。

\chapterroute{日志区域 $\rightarrow$ credit 预留 $\rightarrow$ 四次 FLUSH
$\rightarrow$ 断电状态 $\rightarrow$ 幂等 replay 和再次断电。}

\input{deepening/ch13-ordered-journal}
