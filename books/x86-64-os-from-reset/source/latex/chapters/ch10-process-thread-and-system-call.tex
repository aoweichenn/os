\chapter{让多个线程轮流使用 CPU}

让两个用户程序各打印三行。如果没有调度器，第一个程序会一直运行到退出；
加入 PIT 时钟中断后，内核可以保存当前寄存器和栈，恢复另一个 Thread 的现场。
本章从这次切换区分 Process 与 Thread，再加入阻塞、退出和原生系统调用入口。

\input{topics/ch10-processes-files-system-calls}
\input{deepening/ch10-processes-files-and-shell}
\input{deepening/ch10-process-and-thread}
\input{deepening/ch10-cpu-local-and-syscall}
