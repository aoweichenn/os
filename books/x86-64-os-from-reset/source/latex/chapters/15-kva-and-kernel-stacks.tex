\chapter{内核虚拟地址怎样容纳动态栈}

物理页可以零散，内核对象却常需要连续虚拟地址。本章在高半区管理 KVA 窗口，
按需建立页表，并为每个 Thread 组合 guard page、数据页和首次异常现场。
线程退出以后，栈只能在不再使用它的安全点回收。

\chapterroute{KVA 区间 $\rightarrow$ 页表创建与撤销 $\rightarrow$ 页表根
共享 $\rightarrow$ 动态内核栈 $\rightarrow$ 安全点回收。}

\input{deepening/ch08-kva-and-kernel-stacks}
