\chapter{让多个线程轮流使用 CPU}

让两个用户程序各打印三行。如果没有调度器，第一个程序会一直运行到退出；
加入 PIT 时钟中断后，内核保存当前寄存器和栈，恢复另一个 Thread 的现场。
本章只处理可运行线程、上下文切换、抢占和退出，等待队列留到通信章节。

\chapterroute{进程地址空间 $\rightarrow$ Ready/Running 状态 $\rightarrow$
通用寄存器现场 $\rightarrow$ FPU/SSE 现场 $\rightarrow$ 轮转与退出测试。}

\input{deepening/ch10-processes-files-and-shell}
\input{deepening/ch10-process-and-thread}
