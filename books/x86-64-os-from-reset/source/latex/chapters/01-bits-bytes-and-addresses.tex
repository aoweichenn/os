\chapter{先认识 bit、byte 和地址}

数一排开关，我们会得到状态数量；给一排存储格编号，我们会得到地址。程序、
字符和机器指令最终都要保存成字节。本章先建立后面马上要用的二进制、十六
进制、字节和地址，再把容量、编码与历史背景接到真实项目。

\chapterroute{bit、byte 与数制 $\rightarrow$ 固定位宽、补码与溢出
$\rightarrow$ 地址、端序与内存转储 $\rightarrow$ 128 KiB ROM 偏移和
真实复位字节 $\rightarrow$ 编码、历史与项目实验。}

\input{foundations/quantities-information-and-history}
