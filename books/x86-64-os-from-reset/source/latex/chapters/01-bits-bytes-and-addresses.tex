\chapter{先认识 bit、byte 和地址}

数一排开关，我们会得到状态数量；给一排存储格编号，我们会得到地址。程序、
字符和机器指令最终都要保存成字节。本章只建立后面马上要用的二进制、十六
进制、字节和地址，不让额外数学挡在第一条指令之前。

\chapterroute{bit 的状态数 $\rightarrow$ byte 与十六进制 $\rightarrow$
地址和容量 $\rightarrow$ 同一组字节的不同解释。}

\input{foundations/mainline/01-bits-bytes-and-addresses}
