\chapter{从磁盘进入 64 位内核}

这一章从串口上的 \code{[OS][STAGE1] ENTERED} 开始，到
\code{[OS][KERNEL] ENTERED} 结束。中间的代码要读磁盘、打开 A20、建立
GDT 和页表、切换处理器模式、检查 ELF64，并把 BootInfo 地址交给 C++
入口。我们会沿日志出现的顺序逐步完成这些工作。

\chapterroute{Stage 1 入口 $\rightarrow$ 读取并检查 ELF $\rightarrow$
打开 A20 与装载 GDT $\rightarrow$ 建立 PAE 页表 $\rightarrow$ 进入 64 位内核}

\input{topics/ch07-long-mode-and-loader}
\input{topics/ch07-stage1-implementation}
\input{deepening/ch07-loader-and-long-mode}
\input{deepening/ch07-boot-memory-wiring-atlas}
